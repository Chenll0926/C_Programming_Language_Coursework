visualise.c

Convert from decimal value to binary value.

Firstly, convert the char type and the unsigned char type into int type.

Then, divide the number by 2 and get the integer quotient into the array.  Finally, reverse output of the array. The length of the array is the size of the data type multiply 8.